name: flutter_kyc
description: KYC Manager App

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^6.0.0
  http: ^1.2.0
  image_picker: ^1.0.7
  intl: ^0.19.0

flutter:
  uses-material-design: true